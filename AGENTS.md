このリポジトリは Zenn の記事・本を GitHub 連携で管理する Zenn Contents リポジトリである。
執筆にあたってはzenn スキルを必ず先に参照すること

## 執筆の流れ

research→outline→article→refineという4フェーズで執筆する

### research phase

執筆対象の概念について理解を深めるフェーズ

- researchフォルダ以下に執筆対象の概念を表す名前のフォルダがまだ無ければ新規作成する
  - 例: Cloudflare Durable Objectのためのフォルダとしてcf-durable-objectフォルダを作成
- そのフォルダ以下にmemo.mdが無ければ新規作成する
- memo.mdでは、執筆対象の概念に関するさまざまなメモやQAが記載される。このファイルはユーザもAI(あなた)も編集を行う
- ユーザからあなたに執筆対象に関する概念についての質問をされた場合、最初にmemo.mdに既に記載されている質問かを確認する。記載されていなければ調査して回答したのちに、今回のQA内容をmemo.mdに追記する
- ユーザからリサーチを指示された場合、最初にテーマに関してさまざまなクエリでWeb検索を行い、どのような観点で記事が書けそうかを検討する
- また、以下のCosenseプロジェクトの内容を参照し、参考になりそうなページを探す。ページ内に記載のリンク先内容も確認する
  - niki-ai
  - niki-auth
  - niki-tech
  - niki-cs

### outline phase

執筆する記事の骨子を検討するフェーズ

- 執筆対象概念フォルダ以下にoutline.mdが無ければ新規作成する
- outline.mdでは、memo.mdの内容をもとに記事にどんな内容を記載するかの骨子を箇条書きで記載する。項目ごとに参照元のURLを末尾に記載する。URLは[参考](...)という形のリンクにする
  - 例
    - Thinkはトップレベルエージェント(WebSocket経由で`useAgentChat`と通信)とサブエージェント(RPC経由で親エージェントが`chat()`で駆動)の両方として動作する
      - 参考: [https://developers.cloudflare.com/agents/harnesses/think/](https://developers.cloudflare.com/agents/harnesses/think/)
- 原則としてoutline.mdはユーザが編集するが、ユーザの指示に応じてAI(あなた)が編集しても良い

### article phase

記事を執筆するフェーズ

#### 執筆時の文章ルール

- ですます調で記載
- 太字記法(\*\*で囲む)は禁止
- 「推奨」、「劇減」、「必須」などの解釈・強調・比喩を含めず、事実のみを記載する
- `---`によるセクション区切りは禁止
- サービスの仕様などに言及する際は、参照元のURLをインライン脚注として記載し、一言コメントを添える
- 一文が短い箇条書きでサマリを書かない。
  - よくない例
    - 動的生成とスタブ取得:
    `env.COUNTERS.getByName(name)`で参照すると、指定した名前に対応するDOインスタンスが配置・初期化されます。
  - 良い例
    - `env.COUNTERS.getByName(name)`で参照すると、指定した名前に対応するDOインスタンスが配置・初期化されます。

具体例としてはarticles/9a9769410a26d8.mdを参照

### refine phase

記事の内容を精査するフェーズ

- 記事がarticle phaseの執筆時の文章ルールに則っているかをチェックする
- 末尾に「参考」セクションが無い場合追加する。このセクションでは、記事から参照した外部URLを列挙する
  - 具体例としてはarticles/9a9769410a26d8.mdを参照
- 記載された内容が正しいかどうかを参照されている外部URLの参照やweb検索により確認する
- 記事の内容に対してインライン脚注で参照されている外部URLが正しいかどうかをチェックする


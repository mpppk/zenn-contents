このリポジトリは Zenn の記事・本を GitHub 連携で管理する Zenn Contents リポジトリである。
執筆にあたってはzenn スキルを必ず先に参照すること

## 執筆時のルール

- ですます調で記載
- 太字記法(**で囲む)は禁止
- 「推奨」、「劇減」、「必須」などの解釈・強調・比喩を含めず、事実のみを記載する
- `---`によるセクション区切りは禁止
- サービスの仕様などに言及する際は、参照元のURLをインライン脚注として記載し、一言コメントを添える
- 一文が短い箇条書きでサマリを書かない。
  - よくない例
    - 動的生成とスタブ取得:
      `env.COUNTERS.getByName(name)`で参照すると、指定した名前に対応するDOインスタンスが配置・初期化されます。
  - 良い例
    - `env.COUNTERS.getByName(name)`で参照すると、指定した名前に対応するDOインスタンスが配置・初期化されます。

